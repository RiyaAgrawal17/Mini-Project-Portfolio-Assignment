import React from "react";
function Skilldata(props) {
  
  return (

<img className="photo" alt={props.alt} src={props.image}
  style={{ marginTop: "5px" }}
></img>


);
}

export default Skilldata;